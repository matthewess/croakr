- provide :title, @user.name
.row
  aside.col-md-3
    section.sidebar
      h2= @user.name
      p= @user.email
  .col-md-6
    h2 croaks
      - if @user.croaks.any?
        = will_paginate @croaks
        ol.croaks
          = render @croaks
      - else
        p this user hasn't croaked yet!